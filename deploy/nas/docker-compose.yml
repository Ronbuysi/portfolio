services:
  portfolio:
    image: nginx:1.27-alpine
    container_name: wcc-portfolio
    user: "0:0"
    restart: unless-stopped
    ports:
      - "8088:80"
    volumes:
      - ./site:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
