# Operation System Extension Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Turn project 001 from a poster gallery into an operation-design case study with physical activation and digital campaign systems.

**Architecture:** Add `story: 'operation'` and extension data, create a focused `OperationStory` renderer, dispatch it from SelectedWork, and share the case inset and responsive system.

**Tech Stack:** React 19, Vite 8, Vitest, CSS Grid, built-in image generation.

## Task 1: Model and render with TDD

- [ ] Add failing tests for `story: 'operation'`, three originals and two AI extensions.
- [ ] Add operation extension data and create `OperationStory.jsx`.
- [ ] Dispatch from SelectedWork and preserve all original posters.

## Task 2: Style and verify

- [ ] Add failing CSS tests for three original columns, a visual-grammar module, 16:9 extensions and mobile single columns.
- [ ] Implement dark campaign styling and source labels.
- [ ] Run full tests, build and browser verification.
