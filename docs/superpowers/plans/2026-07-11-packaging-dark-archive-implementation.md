# Packaging Dark Archive Redesign Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace four bright packaging mockups with a dark archival product family and add a truthful AI-assisted rigid gift-box extension based on the original dieline.

**Architecture:** Keep the existing PackagingStory narrative and compatible dark assets. Replace only application media, add one third dieline record, update source labels, and reshape the application grid without changing the project’s shared inset.

**Tech Stack:** React 19, Vite 8, Vitest, CSS Grid, built-in image generation.

## Task 1: Generate four dark application assets

- [ ] Use the original bag and brick mockups as edit targets for dark archival environments while preserving packaging structure and artwork.
- [ ] Generate the open four-compartment rigid gift box from the supplied heaven-and-earth box dieline and existing packaging palette.
- [ ] Inspect, resize to 2048×1152 sRGB PNG and save under `public/images/packaging-redesign/`.
- [ ] Commit with `feat: create dark packaging application set`.

## Task 2: Add media data with TDD

- [ ] Add failing content tests requiring four new application paths, truthful AI source labels and three dielines.
- [ ] Update `projects.js` with the new assets and add the original heaven-and-earth box dieline.
- [ ] Copy and convert `天地盒刀版图.jpg` to `dieline-gift-box.jpg`.
- [ ] Run focused tests and commit with `feat: extend packaging structure evidence`.

## Task 3: Refine application and evidence layouts with TDD

- [ ] Add failing CSS tests for a balanced two-column application grid, contained images and a three-column structure grid.
- [ ] Update PackagingStory image decoding and source labels without changing its six-part narrative.
- [ ] Implement the dark 2×2 grid, 16:9/4:3 frames and mobile single-column behavior.
- [ ] Run focused/full tests and production build, then commit with `style: rebuild packaging application archive`.

## Task 4: Browser verification

- [ ] Verify dark application media, three structure figures, truthful labels and no legacy bright sources.
- [ ] Verify desktop alignment, 390px mobile containment, no overflow and no new console errors.
