export function isGlobalProjectMotionTarget() {
  // Every project figure shares the global motion grammar (am entrance,
  // hover treatment, parallax). NESTA used to be excluded here which made
  // case 007 feel disconnected from the other six case studies.
  return true
}
